# Шаблон корневого .env — `cp .env.template .env` (читает src/olist_ml/defs/env.py). Относительные пути — от корня проекта.
DUCKDB_PATH=data/olist.duckdb
# Файлы окружения (от корня проекта, через запятую) и обязательные переменные — читает defs/env.py.
ENV_FILES=.env,dbt/.env
REQUIRED_ENV_VARS=DUCKDB_PATH,DBT_TARGET,CH_RAW_DB
