# Шаблон dbt/.env — `cp dbt/.env.template dbt/.env` (читает src/olist_ml/defs/env.py; для голого dbt CLI — `set -a; source .env`).
DBT_TARGET=duck
CH_RAW_DB=olist_raw
# ClickHouse (таргет ch, пока не используется) — заполнить при DBT_TARGET=ch.
# CH_HOST=
# CH_PORT=8123
# CH_USER=
# CH_PASSWORD=
# CH_SECURE=false
